apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dragalia-api-postgres-data-persistent-volume-claim
spec:
  volumeName: dragalia-api-postgres-data-persistent-volume
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 16Gi